<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    
    
    <h3><%= dataMovie.name%></h3>
    <p>AddCast :</p>

    <form action="/movies/addCast/<%= dataMovie.id%>" method="POST">
        <label for="">Actor</label><br>
        <select name="first_name" id=""><br>
        <% dataCast.forEach(e =>{ %>
            <option value="<%= e.id%>" ><%= e.getFullName()%></option><br>
        <% })%>
    </select><br><br>

    <label for="">Role</label><br>
    <input type="text" name="role"><br><br>
    <input type="submit" value="submit"><br><br>

    <p>List Cast</p>
    <table border="1">
        <tr>
            <th>Actor</th>
            <th>Role</th>
        </tr>
        <% dataMovie.Casts.forEach(e =>{ %>
        <tr>
            <td><%= e.first_name%></td>
            <td><%= e.MovieCast.role%></td>
                
        <% })%>
        </tr>
    </table>
    </form>
</body>
</html>
